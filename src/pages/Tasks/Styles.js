import styled from 'styled-components'

export const Wrapper = styled.section`
    min-height: 100vh;

    ${'' /* > div:first-child {
        div > div:last-child {
           display: flex;
            justify-content: center;
        }
    } */}
`